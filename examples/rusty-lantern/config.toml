# Rusty Lantern example — every setting is optional; defaults shown in the
# scaffolded template. Uncomment to experiment.

[llm]
# model = "qwen2.5:7b-instruct"
# num_ctx = 8192                    # headroom for the lore file

[overlay]
# enabled = true                    # table display at http://127.0.0.1:8765
